extends browser.pug
block content
  video.playback(autoplay autostart controls width='100%')
    source(src=mp4File type='video/mp4')
    for vttFile in vttFiles
      track(src=vttFile kind='subtitles' srclang='en' label='English')
